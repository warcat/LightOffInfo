@page "/"

<PageTitle>LightOff - Розклад відключень електрики</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app. <span class="oi oi-cog"/>


@inject HttpClient Http
@code {
	protected override async Task OnInitializedAsync()
	{
		var locations = await new Locations("./data/locations.json").ReadLocations(Http);
		if (locations.Any())
			await locations.First().ReadSchedule(Http);
	}
}